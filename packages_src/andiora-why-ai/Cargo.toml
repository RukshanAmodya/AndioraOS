[package]
name = "why"
version = "0.1.2"
edition = "2021"
license = "GPL-3.0-or-later"
description = "why — a fully offline, zero-daemon LLM CLI for Andiora, backed by a bundled Gemma 4 E2B model over llama.cpp"
authors = ["Andiora Team <anduin@aiursoft.com>"]

[[bin]]
name = "why"
path = "src/main.rs"

[dependencies]
llama-cpp-2 = { version = "0.1", features = ["vulkan"] }
clap = { version = "4", features = ["derive", "env"] }
anyhow = "1"
encoding_rs = "0.8"

[profile.release]
strip = true
lto = true
codegen-units = 1
opt-level = 3
