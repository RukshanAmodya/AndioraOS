#!/bin/sh
set -eu
if [ "$#" -ne 1 ]; then
    echo "The storage probe accepts exactly one whole-disk path." >&2
    exit 2
fi
exec /usr/bin/python3 /usr/lib/andiora-installer-beta/storage_probe_cli.py "$1"
